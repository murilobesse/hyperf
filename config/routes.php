<?php

declare(strict_types=1);

/**
 * Este arquivo pode ser mantido vazio ao usar Attributes nos controllers.
 * 
 * As rotas definidas via Attributes são escaneadas automaticamente
 * quando o servidor inicia (desde que scan_cacheable esteja configurado).
 * 
 * Exemplo de uso no controller:
 * #[GetMapping(path: '/users')]
 * #[PostMapping(path: '/users')]
 * #[PutMapping(path: '/users/{id}')]
 * #[DeleteMapping(path: '/users/{id}')]
 */
